# コピーして .env.local を作り、値を入れてください。
# .env.local は .gitignore に入っています。絶対にコミットしないこと。
# 値は Supabase の Project Settings → API から取れます。

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=ここに publishable key

# Secret key はここに書かないこと。Edge Function の中だけで使います。
